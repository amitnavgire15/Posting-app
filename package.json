
{
  "name": "gen-ai",
  "version": "1.0.0",
  "description": "This folder is a sibling to the PlayWright workspace and is for generative-AI experiments, demos, and utilities.",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:report": "npx playwright show-report",

    "mcp:start": "node server.mjs",
    "mcp:dev": "node --watch server.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
